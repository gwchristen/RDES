<UserControl x:Class="RDES.App.Views.ModemDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="750" d:DesignWidth="1100"
             Background="{DynamicResource BackgroundBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="20">

            <!-- Top Header & Modem Health Metrics Summary Cards -->
            <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="16"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Title & State Badge -->
                    <Grid Grid.Row="0">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="📡 Modem Health &amp; Watchdog Dashboard" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <Border Background="#203B82F6" BorderBrush="{DynamicResource PrimaryBrush}" BorderThickness="1" CornerRadius="12" Padding="10,2" Margin="12,0,0,0">
                                <TextBlock Text="{Binding CurrentState}" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource PrimaryBrush}"/>
                            </Border>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <Button Content="🔌 Connect" Style="{StaticResource PrimaryButton}" Command="{Binding ConnectModemCommand}" Margin="0,0,8,0" Padding="10,4"/>
                            <Button Content="⚡ Disconnect" Style="{StaticResource SecondaryButton}" Command="{Binding DisconnectModemCommand}" Margin="0,0,8,0" Padding="10,4"/>
                            <Button Content="🧪 Simulate USB Disconnect" Style="{StaticResource DangerButton}" Command="{Binding SimulateUsbDisconnectCommand}" Margin="0,0,8,0" Padding="10,4"/>
                            <Button Content="♻️ Simulate USB Reconnect" Style="{StaticResource SuccessButton}" Command="{Binding SimulateUsbReconnectCommand}" Padding="10,4"/>
                        </StackPanel>
                    </Grid>

                    <!-- Row 2: 5 Metric Cards -->
                    <UniformGrid Grid.Row="2" Columns="5">
                        <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="RETRY COUNT" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding TotalRetries}" FontSize="22" FontWeight="ExtraBold" Foreground="{DynamicResource PrimaryBrush}" Margin="0,4,0,0"/>
                                <TextBlock Text="Backoff + Jitter retries" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="FAILURES / HOUR" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding FailuresPerHour, StringFormat={}{0:F1}}" FontSize="22" FontWeight="ExtraBold" Foreground="{DynamicResource WarningBrush}" Margin="0,4,0,0"/>
                                <TextBlock Text="Sliding 1-hour window" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="DISCONNECTS / RECOVERIES" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                    <TextBlock Text="{Binding TotalDisconnects}" FontSize="22" FontWeight="ExtraBold" Foreground="{DynamicResource DangerBrush}"/>
                                    <TextBlock Text=" / " FontSize="22" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <TextBlock Text="{Binding TotalRecoveries}" FontSize="22" FontWeight="ExtraBold" Foreground="{DynamicResource SuccessBrush}"/>
                                </StackPanel>
                                <TextBlock Text="USB disconnect events" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,8,0">
                            <StackPanel>
                                <TextBlock Text="UPTIME" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding UptimePercentage, StringFormat={}{0:F1}%}" FontSize="22" FontWeight="ExtraBold" Foreground="{DynamicResource SuccessBrush}" Margin="0,4,0,0"/>
                                <TextBlock Text="Availability percentage" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </Border>

                        <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14">
                            <StackPanel>
                                <TextBlock Text="PORT &amp; LAST CONNECT" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding PortName}" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,4,0,0"/>
                                <TextBlock Text="{Binding LastConnectedText, StringFormat={}Last: {0}}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>
                </Grid>
            </Border>

            <!-- Middle Row: Long-Run Soak Mode & Crash-Safe Batch Sessions -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Column: Long-Run Soak Mode (8-24h) -->
                <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20">
                    <StackPanel>
                        <TextBlock Text="⏱️ Long-Run Soak Test Mode (8–24 Hours)" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                        <TextBlock Text="Runs continuous periodic health checks with exponential backoff &amp; jitter to evaluate modem reliability over long durations." 
                                   FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" Margin="0,0,0,16"/>

                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <TextBlock Text="Target Duration (Hours 8-24):" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding TargetSoakHours, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Check Interval (Seconds):" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding HealthCheckIntervalSeconds, UpdateSourceTrigger=PropertyChanged}" Padding="6,4"/>
                            </StackPanel>
                        </Grid>

                        <CheckBox Content="Stop soak test immediately on failure" IsChecked="{Binding StopSoakOnFailure}" Margin="0,0,0,16"/>

                        <!-- Soak Status Box -->
                        <Border Background="{DynamicResource BackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,16">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid Grid.Row="0">
                                    <TextBlock Text="{Binding SoakStatusText, StringFormat={}Status: {0}}" FontWeight="Bold" FontSize="12" Foreground="{DynamicResource PrimaryBrush}"/>
                                    <TextBlock Text="{Binding SoakElapsedTimeText, StringFormat={}Elapsed: {0}}" HorizontalAlignment="Right" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </Grid>
                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
                                    <TextBlock Text="Total Checks: " FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <TextBlock Text="{Binding SoakTotalChecks}" FontWeight="Bold" FontSize="11" Margin="0,0,12,0"/>

                                    <TextBlock Text="Passed: " FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <TextBlock Text="{Binding SoakPassedChecks}" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}" FontSize="11" Margin="0,0,12,0"/>

                                    <TextBlock Text="Failed: " FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    <TextBlock Text="{Binding SoakFailedChecks}" FontWeight="Bold" Foreground="{DynamicResource DangerBrush}" FontSize="11"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="▶️ Start Soak Test" Style="{StaticResource PrimaryButton}" Command="{Binding StartSoakTestCommand}" Margin="0,0,8,0" Padding="14,6"/>
                            <Button Content="⏹️ Stop Soak Test" Style="{StaticResource DangerButton}" Command="{Binding StopSoakTestCommand}" Padding="14,6"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Right Column: Crash-Safe & Resumable Batch Sessions -->
                <Border Grid.Column="2" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20">
                    <StackPanel>
                        <TextBlock Text="💾 Crash-Safe &amp; Resumable Batch Sessions (SQLite)" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                        
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{Binding NewBatchName}" Padding="6,4" Margin="0,0,8,0" ToolTip="Session Name"/>
                            <Button Grid.Column="1" Content="➕ Create Batch" Style="{StaticResource PrimaryButton}" Command="{Binding CreateBatchSessionCommand}" Padding="10,4"/>
                        </Grid>

                        <TextBlock Text="Serial Numbers (one per line):" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding NewBatchSerialsText, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="60" VerticalScrollBarVisibility="Auto" Margin="0,0,0,12"/>

                        <!-- Batch List -->
                        <TextBlock Text="Persisted Batch Sessions in SQLite:" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,4"/>
                        <DataGrid ItemsSource="{Binding BatchSessions}" SelectedItem="{Binding SelectedBatchSession}" AutoGenerateColumns="False" IsReadOnly="True" Height="100" Margin="0,0,0,12" HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="80"/>
                                <DataGridTextColumn Header="Progress" Binding="{Binding ProcessedItems, StringFormat={}{0}}" Width="60"/>
                                <DataGridTextColumn Header="Total" Binding="{Binding TotalItems}" Width="50"/>
                                <DataGridTextColumn Header="Success" Binding="{Binding SuccessCount}" Width="60"/>
                                <DataGridTextColumn Header="Failed" Binding="{Binding FailureCount}" Width="50"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <DockPanel>
                            <TextBlock Text="{Binding BatchStatusText}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                            <Button Content="▶️ Resume Selected Batch" HorizontalAlignment="Right" Style="{StaticResource SuccessButton}" Command="{Binding ResumeBatchSessionCommand}" Padding="12,5"/>
                        </DockPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Bottom Section: Structured Incident Logs & Run Diagnostics Export -->
            <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="20">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="📋 Structured Incident Logs &amp; Diagnostics" FontSize="15" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <ComboBox SelectedValue="{Binding SelectedSeverityFilter}" SelectedValuePath="Content" Width="100" Margin="12,0,0,0">
                                <ComboBoxItem Content="All" IsSelected="True"/>
                                <ComboBoxItem Content="Info"/>
                                <ComboBoxItem Content="Warning"/>
                                <ComboBoxItem Content="Error"/>
                                <ComboBoxItem Content="Critical"/>
                            </ComboBox>
                            <Button Content="🔄 Refresh Logs" Style="{StaticResource SecondaryButton}" Command="{Binding RefreshLogsCommand}" Margin="8,0,0,0" Padding="8,3"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <Button Content="📥 Export Run Diagnostics (JSON)" Style="{StaticResource PrimaryButton}" Command="{Binding ExportDiagnosticsCommand}" Padding="12,5"/>
                        </StackPanel>
                    </Grid>

                    <!-- Incident Logs Table -->
                    <DataGrid ItemsSource="{Binding IncidentLogs}" AutoGenerateColumns="False" IsReadOnly="True" Height="180" HeadersVisibility="Column">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="140"/>
                            <DataGridTextColumn Header="Severity" Binding="{Binding Severity}" Width="80"/>
                            <DataGridTextColumn Header="Event Type" Binding="{Binding EventType}" Width="120"/>
                            <DataGridTextColumn Header="Port" Binding="{Binding PortName}" Width="70"/>
                            <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                            <DataGridTextColumn Header="Exception" Binding="{Binding ExceptionDetails}" Width="200"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <TextBlock Text="{Binding ExportDiagnosticsStatus}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}" Margin="0,8,0,0"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
